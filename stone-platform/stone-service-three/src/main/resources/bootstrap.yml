server:
  port: 8003
  servlet:
    context-path: /service-three
spring:
  cloud:
    nacos:
      discovery:
        server-addr: ${nacos.server-addr.${spring.profiles.active}}
        namespace: ${nacos.config.namespace.${spring.profiles.active}}
      config:
        server-addr: ${nacos.server-addr.${spring.profiles.active}}
        file-extension: yml
        namespace: ${nacos.config.namespace.${spring.profiles.active}}
  application:
    name: service-three
  profiles:
    active: dev
nacos:
  config:
    namespace:
      dev: b03acc92-d77d-4ca6-aa8c-99291a520839
  server-addr:
    dev: localhost:8848